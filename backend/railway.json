{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --production && npx --yes prisma generate && npx --yes tsc -p tsconfig.json"
  },
  "start": {
    "command": "node dist/index.js"
  }
}