{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --production && npm run prisma:generate && npm run build"
  },
  "start": {
    "command": "npm start"
  }
}