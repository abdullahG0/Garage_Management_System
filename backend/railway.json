{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npx -p prisma prisma generate && npx -p typescript tsc -p tsconfig.json"
  }
}